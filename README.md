# spelling-correction-system

The system uses a hybrid approach combining FastText embeddings for semantic similarity, FAISS for efficient nearest-neighbor search, and fuzzy logic (Levenshtein Distance or Jaro-Winkler Similarity) for character-level filtering. This ensures both semantic and syntactic accuracy when correcting misspellings.

